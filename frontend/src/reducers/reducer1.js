// Name is placeholder

/**
 * @param {unknown[]} [state]
 * @param { { type: "ACTION_1" } } action
 */
export default (state = [], action) => {
  switch (action.type) {
    case "ACTION_1":
      return state;
    default:
      return state;
  }
};
